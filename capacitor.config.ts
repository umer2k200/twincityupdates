import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.twincityupdates.app',
  appName: 'Twin City Updates',
  webDir: 'www'
};

export default config;
